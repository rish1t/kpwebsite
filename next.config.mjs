/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "cdn.example.com", "another-domain.com", "cdn.iconscout.com", "dsm01pap008files.storage.live.com", "centralus1-mediap.svc.ms"],
    },
};

export default nextConfig;
