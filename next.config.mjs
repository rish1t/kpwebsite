/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "cdn.example.com", "another-domain.com", "cdn.iconscout.com"],
    },
};

export default nextConfig;
